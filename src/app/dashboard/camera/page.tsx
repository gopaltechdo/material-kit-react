

import * as React from 'react';

import { Camera } from '@/components/dashboard/camera/CameraDetails';

export default function Page(): React.JSX.Element {
    return (
    <div>
    <Camera>

    </Camera>
    </div>
    );
  }
  